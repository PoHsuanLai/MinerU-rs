# Rust build artifacts
/target/
**/*.rs.bk

# Cargo lock is kept (workspace with binaries)

# Local editor / OS
.DS_Store
*.swp

# Regenerable ONNX reference dumps for parity tests (see the .py dumpers)
crates/mineru-table/tests/reference/*.bin
crates/mineru-table/tests/reference/*.shape
